datalake_transformations:
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: "datalake"
      schema: "main"
      schemas_and_paths:
        main: "../datalake.db"
      schema_directory: ".."
    prod:
      type: sqlite
      threads: 1
      database: "datalake"
      schema: "main"
      schemas_and_paths:
        main: "../datalake.db"
      schema_directory: ".."
  target: dev
